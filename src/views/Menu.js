/*
                *H1*
    add-btn | {menu.item} | {item.price}
    {menu.item.description}
*/
import MenuList from "../components/MenuList";

function Menu() {
  return (
    <section>
      <MenuList></MenuList>
    </section>
  );
}

export default Menu;
